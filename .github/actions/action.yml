name: "Greeting with js"
inputs:
  username:
    type: string
    description: "username to greet"
    default: "world"
  greeting: string
    description: "salutation"
    default: "Hello"
outputs:
  message:
    description: "Message generated by greeting script"
runs:
  using: "node20"
  main: "index.js"
